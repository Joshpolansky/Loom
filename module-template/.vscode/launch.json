{
  // Run and debug the Loom runtime.
  //
  // Prerequisites:
  //   1. conan install . --build=missing
  //   2. cmake --preset conan-debug
  //   3. In CMake Tools, select "loom" as the launch target
  //
  // The `loom` executable is built from your own main.cpp (which calls
  // loom::run()) and linked against loom::runtime from the loom-runtime
  // Conan package. CMake Tools sees it as a native target — no hacks needed.
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Loom",
      "type": "cppdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": [
        "--module-dir", "${workspaceFolder}/build/Debug/modules",
        "--data-dir",   "${workspaceFolder}/data",
        "--port",       "8080"
      ],
      "cwd": "${workspaceFolder}",
      "MIMode": "lldb",
      "setupCommands": [
        {
          "description": "Enable pretty-printing",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ]
    }
  ]
}
